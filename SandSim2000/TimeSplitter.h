#pragma once
#include <chrono>

class TimeSplitter
{
	// currentTime = getTime()
	// previousTime = currentTime
	// deltaTime = currentTime - previousTime
	// accumulator += deltaTime
	// gameIsRunning

	// shouldRenderFrame(deltaTime, gameIsRunning)
	// shouldStepLogic(deltaTime, gameIsRunning)	
};
